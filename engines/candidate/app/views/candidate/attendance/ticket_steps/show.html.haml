#header
  .columns
    .column.text-center
      %p.subtitle.is-5 Atendimento
#content
  .container
    .columns
      .column
        = link_to "Voltar para atendimento".html_safe, candidate.attendance_ticket_steps_path(@ticket), class: 'button is-dark is-fullwidth', data: { disable_with: t(:disable_with)}
    